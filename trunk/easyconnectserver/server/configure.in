AC_INIT(configure.in)
AC_SEARCH_LIBS([ioperm], [ioperm])

PACKAGE=scoped
VERSION=0.1.0

AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

AC_SUBST(INTI_CFLAGS)
AC_SUBST(INTI_LIBS)

AC_LANG_C
AC_PROG_CC
AC_PROG_RANLIB

AC_OUTPUT(Makefile src/Makefile  src/tcpcliserver/Makefile src/tcpcliserver/cl/Makefile)
